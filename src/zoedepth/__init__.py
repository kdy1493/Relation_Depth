"""ZoeDepth integration: fine-tune and evaluate on custom RGB–depth pairs.

Upstream code lives under ``src/zoedepth/vendor`` (clone the ZoeDepth repo there; see ``configs/syntable_zoedepth_0.yaml``).
"""
